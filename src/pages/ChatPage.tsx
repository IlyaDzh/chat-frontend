import React from "react";

import { Dialogs, Messages } from "../components";

export const ChatPage: React.FC = () => {
    return (
        <>
            <Dialogs />
            <Messages />
        </>
    );
};
